import type { Metadata } from "next"

export const metadata: Metadata = {
  title: "Workspace · mu8ic",
}

export default function WorkspaceLayout({ children }: { children: React.ReactNode }) {
  return children
}
